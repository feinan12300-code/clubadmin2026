.container { padding: 24rpx; box-sizing: border-box; }

.empty {
  text-align: center;
  padding: 100rpx 24rpx;
  color: #9ca3af;
}
.empty-icon { font-size: 80rpx; margin-bottom: 20rpx; opacity: 0.85; }
.empty > view:not(.empty-icon) {
  font-size: 32rpx;
  font-weight: 600;
  color: #1f2937;
}

/* 通知卡片：圆角 24rpx、柔和阴影 */
.notice-card {
  background: #fff;
  border-radius: 24rpx;
  padding: 28rpx 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.06);
  border: 1rpx solid #f3f4f6;
}
.notice-card.pending {
  border-left: 6rpx solid #f59e0b;
}

.nc-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}
.nc-table {
  font-size: 32rpx;
  font-weight: 700;
  color: #111827;
}
.nc-status {
  font-size: 22rpx;
  padding: 4rpx 14rpx;
  border-radius: 999rpx;
  font-weight: 500;
}
.nc-status-pending {
  background: #fef3c7;
  color: #92400e;
}
.nc-status-handled {
  background: #d1fae5;
  color: #065f46;
}

.nc-body {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
}
.nc-amount {
  font-size: 40rpx;
  font-weight: 700;
  color: #ef4444;
}
.nc-time {
  font-size: 24rpx;
  color: #9ca3af;
}

/* 标记已处理按钮：紫色渐变 + 主色阴影 */
.nc-action {
  margin-top: 20rpx;
  text-align: center;
  padding: 16rpx;
  background: linear-gradient(135deg, #6366f1, #818cf8);
  color: #fff;
  font-size: 26rpx;
  font-weight: 600;
  border-radius: 12rpx;
  box-shadow: 0 6rpx 20rpx rgba(99, 102, 241, 0.4);
}
.nc-action:active { transform: scale(0.97); }
